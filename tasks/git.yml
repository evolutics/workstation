- name: Install Git
  apt:
    name: git
  become: true
- name: Configure Git
  git_config:
    name: "{{ item.name }}"
    scope: global
    value: "{{ item.value }}"
  with_items:
    - name: user.email
      value: benjamin.fischer@evolutics.info
    - name: user.name
      value: Benjamin Fischer
    - name: core.editor
      value: code --wait
- name: Install gitk
  apt:
    name: gitk
  become: true
